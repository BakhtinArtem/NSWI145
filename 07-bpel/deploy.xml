<deploy xmlns="http://www.apache.org/ode/schemas/dd/2007/03"
	xmlns:bpel="http://org/example/bpel" 
	xmlns:wsdl="http://org/example/wsdl">
<!-- 
	Get order (check that it exist) and return new license (check that it exist)
	1. get order and return its data
 -->
	<process name="bpel:LicenseRobotProcess">
		<active>true</active>
		<provide partnerLink="mainPartnerLink">
			<service name="wsdl:MainService" port="MainPort"/>
		</provide>
	</process>

</deploy>